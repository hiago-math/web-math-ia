services:
  math-ia-frontend:
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: dev
    container_name: math-ia-frontend
    volumes:
      - ./src:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    networks:
      - fintools
    restart: unless-stopped
    env_file:
      - src/.env
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.math-ia-frontend.rule=Host(`math-ia.localhost`)"
      - "traefik.http.routers.math-ia-frontend.entrypoints=web"
      - "traefik.http.services.math-ia-frontend.loadbalancer.server.port=5173"
      - "traefik.docker.network=fintools"

networks:
  fintools:
    external: true
    name: fintools
